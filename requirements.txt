# Runtime dependencies for the Azure Function App.
# Azure (Flex Consumption) installs these remotely via Oryx during deployment.
# Keep this list to what the app imports at runtime — test/dev tools live in
# requirements-dev.txt so they are not installed on the Function App.

# Azure Functions Python worker
azure-functions==1.21.3

# Azure Communication Services (WhatsApp / Advanced Messaging) — used by services/acs_service.py
azure-communication-messages==1.1.0

# Azure core (transitive for the SDKs above; pinned for reproducibility)
azure-core==1.41.0

# Azure Identity (kept for future Managed Identity use in production)
azure-identity==1.19.0

# HTTP client — used by services/logic_app_service.py
httpx==0.27.0

# Observability -> Application Insights
opencensus-ext-azure==1.1.13

# Load local.settings.json style env vars when running locally
python-dotenv==1.0.1
